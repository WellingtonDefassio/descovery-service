server:
  port: 8010

spring:
  application:
    name: ms-discovery
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

logging:
  level:
    com:
      netflix:
        eureka: OFF
      discovery: OFF
